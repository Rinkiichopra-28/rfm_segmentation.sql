# 🎯 B2B Customer RFM Segmentation & Analytics

Here is the SQL code used to build the customer segments:

```sql
CREATE TABLE sales_data (
    ORDERNUMBER INT,
    QUANTITYORDERED INT,
    PRICEEACH DECIMAL(10, 2),
    SALES DECIMAL(10, 2),
    ORDERDATE DATETIME
);
```
